[project]
name = "admin-panel-api"
version = "1.0.0"
description = "FastAPI backend gateway for the admin panel"
requires-python = ">=3.14"
dependencies = [
  "fastapi==0.136.1",
  "uvicorn[standard]==0.46.0",
  "httpx==0.28.1",
  "pydantic-settings==2.14.1",
  "python-multipart==0.0.28"
]

[tool.uvicorn]
factory = false
